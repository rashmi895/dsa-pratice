package string;

public class pallindrome {
    s=s.toLowercase().replaceAll(regex:[^A-Za-z0-9]);
    int i=0;
    int j=s.length()-1;
    while(i<j){
        if(charAt(i)!=charAt(j)){
        return false;
    }
        i++;
        j--;
    }
    return true;
   
    
}

